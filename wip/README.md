# wip/ — the dual-deployment MCP design series (cold-start guide)

Written 2026-07-17. This directory holds the working design for the **future of `pipelex-mcp` and its distribution** — a series of discussion docs produced in one design session, meant to be resumed cold. Read this file first; it tells you what was decided, what was verified, what's open, and in which order to read the rest. (`pr-5-review-notes.md` is unrelated to this series.)

**Ground rule for resuming**: `SPEC.md` and `CLAUDE.md` describe the *implemented present* of this repo; the docs in this series describe the *agreed future*. **Update 2026-07-20:** the **local shell (workshop)** — next-step item 3 — is now implemented on `feature/Local-server` (the npm-distributed `@pipelex/mcp` stdio server, the `{ path }` arm, the trust boundary, packaging; tools-first, no local views). Before that, only the Skybridge 1.2.7 upgrade had shipped. **Update 2026-07-21:** **catalog run-by-reference is now implemented too** (on `feature/method-id-catalog-runs` — queue item 4's catalog portion; `method_id` on `mthds_run` via the platform's native `/v1/start` resolution and on `mthds_inputs_template` via fetch-and-forward). Everything else in the series (catalog discovery tools, methods-as-tools, console OAuth, the plugin follow-through) is still *agreed future*. Docs 1 and 2 carry dated revision notes; where any doc conflicts with doc 4, doc 4 wins.

## What we're designing, in one paragraph

`pipelex-mcp` will ship as **two servers from one repo and one capability core**: the existing hosted Skybridge server (the **console** — ChatGPT, claude.ai, Cowork consumers) and a new npm-distributed local server (the **workshop** — Claude Code, Codex, Cursor). The split exists because MCP tool arguments are LLM-generated, so submitting `.mthds` file contents through the hosted server means the model hand-copies them token by token (slow — a single method build was observed taking over ten minutes — and not byte-accurate); a local server reads `{ path }` from disk instead. The platform's method catalog bridges the two: build in the workshop, publish to the catalog (`mt_<id>`), then the console runs methods **by reference** (the server fetches the stored bundle and forwards it to the runner — content never crosses the LLM), with the endgame that a user's registered methods surface as **first-class MCP tools** their assistant already knows. Views (`run-graph`, `run-follow`) are a shared asset of both servers now that MCP Apps rendering spans Cursor, Cowork, and ChatGPT's Codex mode — Claude Code is the one text-only host. The `pipelex-plugins` plugin does **not** fork: one plugin, per-target server registration.

## Reading order

1. **`dual-deployment-assessment.md`** — the founding decision: the latency diagnosis, why transport (not rules) decides filesystem access, the two-shell architecture, distribution (URL vs npm), the one-host-one-server rule. Carries superseded-view revision notes.
2. **`build-vs-run-dimension.md`** — the lifecycle axis: workshop vs console, the catalog as the bridge, fetch-and-forward, the phase-by-phase map, auth as the console's critical path. Carries revision notes.
3. **`methods-as-tools-discoverability.md`** — dynamic per-session tool projection on the hosted console (each registered method becomes an MCP tool), the awareness gradient (nudges → instructions summary → projection → curation), constraints, and what's parked (per-user deployed MCPs → a future publishing product).
4. **`mcp-apps-landscape-and-local-ui.md`** — the researched July-2026 landscape (MCP Apps official; who renders what), the Skybridge verification (HTTP-coupled at transport *and* view-asset level; single canonical MCP Apps resource since 1.2.0; first-class auth incl. WorkOS since 1.2.0), the local-transport options (localhost HTTP / **stdio launcher + embedded HTTP, recommended** / plain stdio fallback), and the **V1/V2 empirical verification plan with checkpoints**. This doc supersedes stale view claims elsewhere.
5. **`plugin-reconciliation.md`** — why `pipelex-plugins` stays one plugin: per-target server declaration ("one install, one server"), the Claude-target two-persona tension (hosted-by-default + builder opt-in recommended), Codex target goes local, open packaging questions. §7 addendum: the plugin's build targets and the host→server matrix are different tables (artifact default ≠ host intent; Cowork resolved 2026-07-19 as a dual-status console+workshop host, like ChatGPT desktop), plus the drafted host→server matrix.
6. **`auth-design.md`** — auth for both shells. Console: Skybridge 1.2 first-class auth + WorkOS AuthKit-for-MCP; the MCP-side OAuth handshake is now ~config (workosProvider + DCR toggle + resource indicator); the remaining work is a bounded authorizer change on the **platform team's** side (second issuer, `aud` allowlist, route scoping, `X-Org-Id` validation) — §4 of the doc is a self-contained handover ask. Workshop (§6): `plx_sk_` API keys, org-bound so no org handshake; key acquisition reuses the **existing** `pipelex login` loopback-browser flow (`app.pipelex.com/auth/cli`), adapted to mint the platform key instead of the gateway key (W1–W3). Decisions D1–D7 pending (pass-through vs ID-JAG, `aud` policy, route scope, console org UX, entitlement slug, `ff_api_keys` plan gate, mint-time org selection); verification items V-A1–V-A5.

7. **`dual-server-conducted-views.md`** — **candidate strategy, not decided** (recorded 2026-07-21): connect *both* servers to a view-rendering workshop host (Codex, Cowork) and let the Pipelex skill conduct them — local workshop for `{ path }` tools, hosted console for views (https origin, so the localhost penalty never applies and self-contained bundles aren't needed). Variants: re-submit inline (A), **static client-side render via `mthds-ui`'s `buildStaticGraphSpecFromToml` (A′ — preferred cheap path, and the decided *default* display: "show this method" renders the static graph; the validation-issued graph stays available on demand)**, and render-by-reference (B — API-side artifact storage + one hosted tool). Deliberately revisits the "one host, one server" decision for these hosts only.

8. **`console-attachments-landscape.html`** — **research recap, nothing implemented** (recorded 2026-07-30, written as a rendered HTML page for the team): can the hosted console reach the files a user uploads into a chat, given that on chatgpt.com / claude.ai the console is all they have? Answers per host from primary sources. **ChatGPT: yes today** — `_meta["openai/fileParams"]` delivers conversation attachments to a tool call as a signed `download_url` + `file_id` the *server* fetches (bytes never enter the model's context), already wrapped by the Skybridge 1.3.2 we ship (`FileRef`, `useFiles()`). **claude.ai: no channel at all** — uploads stay in model context; only user-pasted URLs and an allowlist-gated sandbox-curl workaround we decline. **MCP spec: nothing shipped** through `2026-07-28`; SEP-2631 (OpenAI-authored, Anthropic-led File Uploads WG) is the draft standard, and its shape — upload out-of-band, pass an opaque URI — is what `mthds_prepare_inputs` already does. Consequence: SPEC.md's conditional deferral of console byte-upload ("until a proper out-of-band attachment channel exists") **is now satisfied on ChatGPT**, and BYOK already settled the whose-storage objection. The doc sizes the increment (a top-level `attachments` param + an `ingestAttachments` seam on `PrepareContext` + one `_meta` line, sketch diff included) and records the open questions — binding convention, the SSRF allowlist on server-side fetch, the ChatGPT-mobile placeholder bug, whether `mthds_run` gets it too. **The completed build plan is archived at `archive/TODOS-chatgpt-file-inputs-2026-07-31.md`**; it records the live-host verification, design, implementation, smoke tests, and documentation pass.

Cross-repo input: `../../pipelex-platform/wip/methods-endpoints-recap.md` — the `/methods` CRUD facts the catalog design relies on (org-scoped, server-minted `mt_<uuid4>`, update-only PUT, **no DELETE by design, no versioning**, derived `description`, stored `input_data`).

## Decisions taken (terse)

- Dual deployment; one capability core; both shells register identical tool names (and keep server key `pipelex`) so skills and prefixes never fork.
- One host, one server — never both; the claude.ai-connector-syncs-into-Claude-Code trap must be documented.
- Hosted `{ path }` submissions are rejected with an instructive `input_domain` error.
- Catalog run-by-reference (and inputs-by-reference) via server-side fetch-and-forward; no new platform routes required; "registered-method runs by catalog id" moves out of SPEC Non-Goals. (Shipped 2026-07-21 with one refinement: `mthds_run` uses the platform's *native* `method_id` resolution on `/v1/start` — no fetch round-trip; fetch-and-forward is the mechanism for `mthds_inputs_template` only, where the build routes have no by-id support.)
- Methods-as-tools via dynamic `tools/list` after per-user auth; curation flag when catalogs grow; skills stay the stable manual (any local catalog snapshot is generated, never authored).
- Views ship on both servers; local shell is the same Skybridge server run locally (needs an HTTP listener for view assets); Claude Code degrades to text.
- Plugin: one logical plugin, per-target server blocks; Claude marketplace target stays hosted-by-default with a builder opt-in to local; Codex target bakes the local launcher. The hosted default on the Claude artifact is a packaging fallback, not Claude Code's operating mode — Claude Code's intended mode is the local workshop (doc 5 §7).
- Skybridge upgraded 1.1.1 → 1.2.7 (2026-07-17), then **1.2.7 → 1.3.2 (2026-07-30)** — both clean, no code changes, check + tests green. 1.3.0 "Drop In" shipped enforced **per-tool `auth`**, which revises doc 6 §1/§5 and adds D8 (BYOK and console OAuth cannot coexist on `/mcp`). Two conclusions there: 1.3.0's *mixed*-auth headline **does not apply to us** — every console tool ends in an authenticated `/v1/*` call, so all of them are gated and `allowsAnonymous` has no correct use (verified against the platform authorizer); and 1.3.x does **not** deliver auth-dependent `tools/list`, so V-A3 / Fred's ask #2 stay open.

## Next steps (the queue for a new session)

1. **V1 complete 2026-07-19** (incl. Cursor https retest + bonus Mistral Vibe cells) — results + checkpoint outcome in doc 4 §4. Headlines: rendering is healthy through a public https origin (Desktop chat newly confirmed, Cowork, Codex, ChatGPT), but **localhost origins are penalized** (Desktop refuses `http://` connector URLs; Codex strips the view over local HTTP); **Cursor is a genuine rendering gap** (no views on either origin); Vibe web is console-shaped with an empty-iframe bug to chase, Vibe TUI is a text-only workshop host. The view-rendering workshop hosts are **Codex and Cowork** (both dual-status console+workshop hosts, both penalizing localhost origins) → tools-first interim posture stands; V2 go/no-go waits on Fred's reply (self-contained view bundles = the path to local views).
2. **Alpic ask — drafted, NOT sent.** `alpic-ask-fred.md` was **rewritten 2026-07-30** against current reality (the first draft predated the shipped workshop and Skybridge 1.3, and proposed a stdio↔localhost-HTTP bridge we deliberately didn't build). Ask priority inverted: **self-contained view bundles is now #1** (V1 showed hosts penalize localhost origins, so a bridge buys no views), blessed stdio is #2 (it would let us delete the hand-rolled shell), plus dynamic `tools/list` (V-A3, restated to distinguish it from 1.3.0's per-tool gating) and stable-URL/custom-domain (V-A5). Send it, then record answers into doc 4 §4.
3. **✅ SHIPPED 2026-07-20 (on `feature/Local-server`).** The workshop landed tools-first over stdio: the npm-distributed `@pipelex/mcp` bin, the shared `{ path }`/contents files union with per-deployment behavior (workshop resolves, console rejects instructively), the `.mthds`-gated cwd-contained trust boundary, tsup packaging + publish lockstep, the SPEC.md Deployments increment, and E2E + latency verification. `SPEC.md`/`CLAUDE.md`/`README.md`/`CHANGELOG.md` document the shipped state; the build log (`TODOS.md`, Phases 0–5) was deleted 2026-07-21 after the build wrapped — full progress log in git history, still-open items in `deferred-workshop-items.md` (queue item 7). What follows is the original design brief, retained for the rationale. **Local shell (workshop) GREEN-LIT 2026-07-19: detailed design, then implementation.** V1 settled the premise — the workshop ships **tools-first over stdio** (the `{ path }` arm is the headline feature; no views at launch). Nothing external blocks this: Fred's reply gates local *views* (V2) only, and D6 (`ff_api_keys` plan gating) gates the onboarding story, not the code — local dev runs with a hand-minted key. The design session must settle: the stdio entrypoint (plain MCP-SDK server over the shared capability core — leading option given B's localhost-asset risk — vs the Skybridge stdio bridge; revisit when Fred answers), schema mechanics (one union `{ path }`/contents schema with per-deployment rejection vs separate registered schemas), the path trust boundary (honor host roots / cwd, never arbitrary absolute paths), npm packaging (name, `bin` wiring, publish machinery, version lockstep with the Alpic deploy), and the SPEC.md increment (next item). Cold-start read for that session: this file, then doc 1 §3 (the two shells + consequences), doc 4 §3–§4 (transport options, V1 matrix, checkpoint outcome + addendum), doc 6 §6 (workshop auth incl. W1–W3); then the repo's `SPEC.md`/`CLAUDE.md`, and invoke the `skybridge` skill per `AGENTS.md`.
4. **SPEC.md increment — workshop portion ✅ DONE (Phase 0); catalog portion ✅ DONE 2026-07-21** (shipped on `feature/method-id-catalog-runs`, reviewed + live-smoked against the dev API). Landed for the catalog portion: `method_id?` on `mthds_run` (native `/v1/start` resolution, `start({ extra: { method_id } })`; files win, id then rides as run-history linkage) and on `mthds_inputs_template` (fetch-and-forward: `getMethod` → mirror-parse the polymorphic `MethodData.mthds` → `buildInputs`, each forwarded file labeled with the method id as provenance); the classification additions (unknown `method_id` 404 → `input_domain`@`method_id`, paywall 402 → generic `config` arm, org-context 400 folded into the by-id `badRequest` hint); "registered-method runs by catalog id" moved out of Non-Goals. **`mthds_validate` by id stays parked with the conducted-views workstream** (doc 7 decides the "show a registered method" story), as do catalog discovery tools and the publish/save tool. Full decision record in git history (`TODOS.md` on the feature branch); `SPEC.md`/`CLAUDE.md`/`README.md`/`CHANGELOG.md` document the shipped state.
5. **✅ DONE 2026-07-20.** Latency split measured and recorded in `dual-deployment-assessment.md` §1 (the open caveat is resolved). Headline: `{ content }` ≈ `{ path }` *at the server*, so the entire through-host penalty is client-side LLM argument emission (scales ~100× with bundle size); API-side dry-run time is a real secondary term for complex bundles (~10 s for an 8-pipe method) that the workshop cannot shrink but is the smaller term.
6. **Plugin follow-through** (`pipelex-plugins`) — **UNBLOCKED 2026-07-20**, **design revised 2026-07-21 (doc 5 §8)** after BYOK shipped in 0.5.0: the hosted-by-default fallback is dead (a shared literal-URL manifest has no channel for a per-user key), so every plugin target bakes the **local workshop launcher** (`npx -y @pipelex/mcp@latest`) and the hosted console becomes a documented connector path, never a plugin declaration; the builder opt-in inverts into a consumer pointer. **Updated 2026-08-04:** that documented path is now the bare connector URL plus an in-host sign-in — the `?api_key=` channel it originally named died with BYOK (item 8), so mirror the current `README.md` wording, not the 0.5.0-era text. The host→server matrix and the connector-sync warning live in this repo's `README.md`; mirror them into the plugin README. Phase tracker: `../../pipelex-plugins/TODOS.md` on `feature/Dual-MCP`. (npm packaging decisions were folded into item 3 and are shipped.)
7. **Deferred workshop-build items** — `deferred-workshop-items.md` (recorded 2026-07-21, when the completed `TODOS.md` build plan was deleted; its full progress log stays in git history): the team-artifact refresh (now to reflect 0.5.0), the gated Phase 6 `login` subcommand (W2, waiting on W1 in `pipelex-app`), and one minor `src/tools.ts` description fix.
8. **Console auth — ✅ SHIPPED** (OAuth in 0.11.0, BYOK removed in the release after it; doc 6 carries the full addendum). The console now authenticates every caller through WorkOS AuthKit and forwards the verified token upstream; per-user OAuth is its **only** posture — no keyless mode, no shared-key fallback, and the entrypoint refuses to boot without both `WORKOS_AUTHKIT_DOMAIN` and `PIPELEX_MCP_RESOURCE_INDICATOR`. How the decisions actually landed: **D8** as neither of its two options — 0.11.0 put OAuth behind a deploy-time env switch (rollback by unsetting a var while prod moved over), and the next release closed it, deleting `hosted/byok.ts` and moving `contextsForRequest` to `hosted/contexts.ts`. **D4** never needed answering: AuthKit's own org picker runs at sign-in, so the token arrives org-bound. **The `X-Org-Id` ask in doc 6 §4-P4 is dead** — the authorizer reads org context from the token claim only and never from a client-supplied header (verified in `handler.py`); what the platform did need was P3's second issuer, which shipped. Note for anyone registering a new environment: the Resource Indicator is the server **origin with a trailing slash**, not the `/mcp` path. Methods-as-tools still follows behind this (needs V-A3: auth-dependent `tools/list` in Skybridge — answered negatively for 1.3.2, open as ask #2 to Alpic).

## Related material

- **Team artifact** (executive brief, kept in sync with the series): https://claude.ai/code/artifact/2c46ec3c-fa0b-4924-bba1-5e4e2a8d547a — private until shared; a future session updates it by passing this URL as `url` to the Artifact tool.
- Skybridge v1.2.0 "Season Pass" release notes (first-class auth, WorkOS; single MCP Apps resource): https://github.com/alpic-ai/skybridge/releases/tag/v1.2.0
- Skybridge v1.3.0 "Drop In" release notes (per-tool `auth` for mixed public/signed-in tools; DevTools deploy button; Agent Skills over MCP; view logs forwarded to the terminal): https://github.com/alpic-ai/skybridge/releases/tag/v1.3.0
- MCP Apps extension overview: https://modelcontextprotocol.io/extensions/apps/overview
