node_modules/
dist/
coverage/
.env*
.DS_Store
*.tsbuildinfo
.skybridge/
.vercel/
.gstack/

# The workshop's tarball carries the repository's README and licence, which
# its prepack script copies in beside the build and its postpack removes.
packages/workshop/README.md
packages/workshop/LICENSE

# This repo is public. Internal working notes live in the private workspace
# at Pipelex/docs/mcp/ — never here. See Pipelex/docs/mcp/_top_priorities.md.
wip/
TODOS.md
